-- https://mumuki.io/exercises/2417-fundamentos-expresiones-la-linea-que-vuelve

procedure Linea(direccion, color, longitud) {
  repeat(longitud) {
    Poner(color)
    Mover(direccion)
  }
  MoverN(longitud, opuesto(direccion))
}
